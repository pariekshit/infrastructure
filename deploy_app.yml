#!/usr/bin/env ansible-playbook

---
- name: Deploy Artifact 
  hosts: "{{env}}"
  become: yes
  become_user: root
  vars_files:
        - "inventory/{{env}}_vars.yml"
  tasks:
        - include: tasks/deployapp.yml
